# Hugging Face Token for model downloads and API access
HF_TOKEN=

# OpenAI API key for proxy endpoint (optional if passed via Authorization header)
OPENAI_API_KEY=

# Redis URL for storing PII replacement mappings (optional, falls back to in-memory)
REDIS_URL=
